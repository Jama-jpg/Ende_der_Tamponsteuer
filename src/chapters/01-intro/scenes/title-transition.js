/* ═══════════════════════════════════════════════
   SCENE — Title transition spacer (s2)
   Pure scroll spacer. The S2→S3 transition itself is auto-played from
   the countdown scene while scrolling is still locked, so this scene
   contributes scroll length only (no init).
═══════════════════════════════════════════════ */

export default {
  id: 's2',
  height: '350vh',
};
